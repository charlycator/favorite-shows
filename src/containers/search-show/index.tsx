import { SearchInput } from '../../components';


const SearchShow = () => {
  return (
    <SearchInput
      label="Search a show"
      placeholder="Type a show" />
  );
};

export default SearchShow;
